#ifndef _DSSTATUSBAR_H
#define _DSSTATUSBAR_H

#include <GL/glut.h>
#include "dsTimeManager.h"

class DSStatusBar {
public:
    DSStatusBar();
    ~DSStatusBar();
    void show();
    void init();
private:
};

#endif
